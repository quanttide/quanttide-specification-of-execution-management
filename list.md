# List（清单/次级法人）

执行记忆的归属主体——一个清单就是一个次级法人：有自己的身份（id）、名字（name）、和一份独立于任何人的执行记忆（tasks）。公司是顶层法人，清单可层层嵌套对齐业务结构。

## 字段

- `id`: 必选，唯一标识（uuid）。
- `name`: 必选，清单名。
- `tasks`: 本法人的执行记忆（Task 数组）。

## 归属规则

清单按业务维度组织，两个平台（浪潮执行云 / 飞书任务）均设四个清单：**默认**（职能跨业务、公司级事务、探索待归类）/ **量潮数据**（客户项目 + 总结和第二大脑）/ **量潮课堂**（第二大脑、课程、学生）/ **量潮云**（产品研发，后续加用户维度）。

归属判定：**任何执行都有归属地**——不属于业务清单的执行进默认清单，不因"不属于哪个业务"而失去被法人记住的资格。

## JSON 示例

```json
{
  "id": "list-qtdata",
  "name": "量潮数据",
  "tasks": ["task-3f2a", "task-9c01"]
}
```
