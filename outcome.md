# Outcome（结果）

任务产生的结果——独立领域模型，承载一项执行的交付结论/产出。复核通过构成下一环节的输入，Outcome 就是这个流转的载体。

## 字段

- `id`: 必选，唯一标识（uuid）。
- `task_ids`: 可选，→ Task.id 数组，关联的任务。**多对多**：一个结果可关联多个任务，一个任务可产生多个结果。
- `title`: 必选，标题（结果的一句话概括）。
- `description`: 必选，描述——结果的展开：交付了什么 / 验收依据是什么 / 对下一环节意味着什么。

## 规则

- 结果与任务是**多对多**关系（task_ids 关联），不内嵌于 Task——Task 管执行过程，Outcome 管交付结论。
- 复核通过后回填：复核人验收的就是 Outcome，验收通过它即成为下一环节的输入。

## JSON 示例

```json
{
  "id": "9b2f8c3d-1a4e-4f5b-8c6d-7e0a9b1c2d3e",
  "task_ids": [
    "7c9e6679-7425-40de-944b-e07fc1f90ae7"
  ],
  "title": "口径确认单验收通过",
  "description": "客户书面确认算例与边界条件，口径确认单定稿归档，作为二期数据开发的输入。"
}
```
